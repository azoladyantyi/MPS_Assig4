   
   public class Word{
   public static void letters(string word){
       word = word.Replace(" ", string.Empty);
	  int counter;
        //     // Create for loop
       while (word.Length>0)
         {
            counter = 0;
            for (int i = 0; i < word.Length; i++)
          {
               if (word[0] == 
               
               word[i])
               {
                     counter++;
              }
          }
       }
        }
        }